import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitorjs.Duocart',
  appName: 'Duocart',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
